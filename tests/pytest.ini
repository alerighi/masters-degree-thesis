;; pytest configuration file
;; this file controls the options for the test runner

[pytest]
;; add command line options, such as the firmware file
addopts=--firmware-path test.bin

;; always show logs on the cli
log_cli=true
log_level=DEBUG

